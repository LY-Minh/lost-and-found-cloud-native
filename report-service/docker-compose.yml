services:
  report-service:
    build: .
    container_name: report-service
    ports:
      - "3004:3004"
    environment:
      - PORT=3004
      # For local dev: point to localhost or other services
      - CATALOG_SERVICE_URL=http://localhost:3002
      - CLAIMS_SERVICE_URL=http://localhost:3003


