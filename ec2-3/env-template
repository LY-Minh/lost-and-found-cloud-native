# ─────────────────────────────────────────────────────────────
# EC2-3 environment variables
# Copy this file to .env and fill in your MongoDB Atlas credentials
#   cp env-template .env
# ─────────────────────────────────────────────────────────────
# Shared JWT secret — MUST match all other EC2 instances
JWT_SECRET=lost-and-found-secret-key

# MongoDB Atlas connection strings
CATALOG_DB_URI=mongodb+srv://ming:ming@catalog-db.69st1lo.mongodb.net/?appName=catalog-db

# Report-service calls claims-service on EC2-2 (catalog-service is
# co-located and reached via Docker internal DNS, no config needed).
# Use EC2-2's private IP if in the same VPC, otherwise public IP.
CLAIMS_SERVICE_URL=http://172.31.28.229:3003

